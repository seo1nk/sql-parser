// use kernel::parser::Parser;

// /// `identifier` で認識した識別子をSQLキーワードとして認識するパーサー
// pub fn keyword() -> Parser<()> {
//     Parser(Box::new(|input| {
//         let (word, remaining) = identifier().run(input)?;
//     }))
// }
